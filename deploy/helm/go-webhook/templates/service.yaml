apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-webhook.fullname" . }}
  labels:
    {{- include "go-webhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.adminPort }}
      targetPort: admin
      protocol: TCP
      name: admin
  selector:
    {{- include "go-webhook.selectorLabels" . | nindent 4 }}
